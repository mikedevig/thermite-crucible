syntax = "proto3";
package xray.proxy.vless;

// Minimal, wire-compatible stub of Xray-core's vless.Account (fields we set only).
message Account {
  string id = 1;
  string flow = 2;
  string encryption = 3;
}
